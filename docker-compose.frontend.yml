services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ai_gift_assistant_frontend
    ports:
      - "5986:80"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - monitoring_net
    restart: unless-stopped

networks:
  monitoring_net:
    external: true
